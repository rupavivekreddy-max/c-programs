#include <stdio.h>
int main ()
{
    int a, b, c;
    printf("Enter a & b values:");
    scanf("%d %d", &a, &b);
    c = a + b;
    printf("sum of two numbers = %d\n", c);
    return 0;
}

